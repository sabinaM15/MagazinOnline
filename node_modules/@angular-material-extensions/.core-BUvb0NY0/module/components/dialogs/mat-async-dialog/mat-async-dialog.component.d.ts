import { OnInit } from '@angular/core';
import { MatDialogRef, ThemePalette } from '@angular/material';
export interface MatAsyncDialogData {
    title?: string;
    icon?: string;
    color?: ThemePalette;
}
export declare class MatAsyncDialog implements OnInit {
    dialogRef: MatDialogRef<MatAsyncDialog>;
    data: MatAsyncDialogData;
    title: string;
    icon: string;
    color: ThemePalette;
    constructor(dialogRef: MatDialogRef<MatAsyncDialog>, data: MatAsyncDialogData);
    ngOnInit(): void;
}
