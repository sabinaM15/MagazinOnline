import { OnInit } from '@angular/core';
import { MatDialogRef, ThemePalette } from '@angular/material';
export interface MatRadioDialogData {
    title?: string;
    icon?: string;
    color?: ThemePalette;
    textButton?: string;
    options: {
        value: any;
        viewValue?: string;
    }[];
}
export declare class MatRadioDialog implements OnInit {
    dialogRef: MatDialogRef<MatRadioDialog>;
    data: MatRadioDialogData;
    title: string;
    icon: string;
    color: ThemePalette;
    textButton: string;
    options: {
        value: any;
        viewValue?: string;
    }[];
    constructor(dialogRef: MatDialogRef<MatRadioDialog>, data: MatRadioDialogData);
    ngOnInit(): void;
}
