import { OnInit } from '@angular/core';
import { MatDialogRef, ThemePalette } from '@angular/material';
export interface MatLoadingDialogData {
    title?: string;
    icon?: string;
    color?: ThemePalette;
}
export declare class MatLoadingDialog implements OnInit {
    dialogRef: MatDialogRef<MatLoadingDialog>;
    data: MatLoadingDialogData;
    title: string;
    icon: string;
    color: ThemePalette;
    constructor(dialogRef: MatDialogRef<MatLoadingDialog>, data: MatLoadingDialogData);
    ngOnInit(): void;
}
