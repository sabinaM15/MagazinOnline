import { OnInit } from '@angular/core';
import { MatDialogRef, ThemePalette } from '@angular/material';
export interface MatInputDialogData {
    title?: string;
    color?: ThemePalette;
    icon?: string;
    appearance?: string;
    placeholder?: string;
    textButton?: string;
}
export declare class MatInputDialog implements OnInit {
    dialogRef: MatDialogRef<MatInputDialog>;
    data: MatInputDialogData;
    title: string;
    icon: string;
    color: ThemePalette;
    placeholder: string;
    textButton: string;
    constructor(dialogRef: MatDialogRef<MatInputDialog>, data: MatInputDialogData);
    ngOnInit(): void;
}
