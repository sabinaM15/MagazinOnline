import { OnInit } from '@angular/core';
import { MatDialogRef, ThemePalette } from '@angular/material';
export interface MatTimerDialogData {
    title?: string;
    icon?: string;
    color?: ThemePalette;
}
export declare class MatTimerDialog implements OnInit {
    dialogRef: MatDialogRef<MatTimerDialog>;
    data: MatTimerDialogData;
    title: string;
    icon: string;
    color: ThemePalette;
    constructor(dialogRef: MatDialogRef<MatTimerDialog>, data: MatTimerDialogData);
    ngOnInit(): void;
}
