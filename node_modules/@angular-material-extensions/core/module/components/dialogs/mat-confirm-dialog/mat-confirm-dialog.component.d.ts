import { OnInit } from '@angular/core';
import { MatDialogRef } from '@angular/material';
export interface MatConfirmDialogData {
    title?: string;
    confirmMessage: string;
    confirmTextButton?: string;
    cancelTextButton?: string;
}
export declare class MatConfirmDialog implements OnInit {
    dialogRef: MatDialogRef<MatConfirmDialog>;
    data: MatConfirmDialogData;
    title: string;
    confirmMessage: string;
    confirmTextButton: string;
    cancelTextButton: string;
    constructor(dialogRef: MatDialogRef<MatConfirmDialog>, data: MatConfirmDialogData);
    ngOnInit(): void;
}
