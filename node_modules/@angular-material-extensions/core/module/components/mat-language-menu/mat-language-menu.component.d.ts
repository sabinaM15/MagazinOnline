import { EventEmitter, OnInit } from '@angular/core';
export interface Language {
    id: string;
    title: string;
    flag: string;
}
export declare class MatLanguageMenuComponent implements OnInit {
    languages: Language[];
    selectedLanguage: Language;
    onLanguageChanged: EventEmitter<string>;
    constructor();
    ngOnInit(): void;
    /**
     * Set the language
     *
     * @param lang
     */
    setLanguage(lang: Language): void;
}
